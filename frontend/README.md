# Smart Parking System (Vue3)

## Run (dev)
From `frontend/`:
```bash
npm install
npm run dev
```

Frontend默认请求后端地址：`http://localhost:8080`。

## Login
- Admin: `admin / admin123`
- Repair worker: `repair / repair123`

