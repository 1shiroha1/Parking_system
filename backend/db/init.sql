-- 运行前请用 root(123456) 登录 MySQL
CREATE DATABASE IF NOT EXISTS parking_system
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- 数据库表由 Hibernate 在后端启动时自动创建（spring.jpa.hibernate.ddl-auto=update）

